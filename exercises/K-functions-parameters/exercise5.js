// Declare your function here
function createLongGreeting(name, age) {
  let soy = "Hello my name is " + name + " and I'm " + age + " years old";
  return soy;
}
const greeting = createLongGreeting("Daniel", 30);

console.log(greeting);
